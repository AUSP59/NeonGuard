
# Reproducibility

- Pin compilers/toolchains in CI.
- Avoid network access during build.
- Use `-Werror=format-security` and deterministic flags.
- Prefer release artifacts via CI with checksums and SBOM.

apiVersion: v1
kind: Service
metadata:
  name: {{ include "neonsec.fullname" . }}
spec:
  type: ClusterIP
  selector: { app: {{ include "neonsec.name" . }} }
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}

# Data Retention
- The CLI does not persist input by default.
- Optional outputs (JSON/NDJSON/SARIF) are written only when requested.
- Logs/metrics contain aggregates, not raw payloads. Redaction option available.

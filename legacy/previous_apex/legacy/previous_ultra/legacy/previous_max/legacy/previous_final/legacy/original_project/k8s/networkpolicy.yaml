apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata: { name: neonsec-deny-egress }
spec:
  podSelector: { matchLabels: { app: neonsec } }
  policyTypes: ["Egress"]
  egress: []  # deny all egress
